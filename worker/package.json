{
  "name": "cloudflare-ase-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker for the ASE take-home assessment.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute ase-flags-db --file=schema.sql",
    "db:init:remote": "wrangler d1 execute ase-flags-db --remote --file=schema.sql"
  },
  "dependencies": {
    "jose": "^5.6.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240725.0",
    "typescript": "^5.5.3",
    "wrangler": "^4.86.0"
  }
}
